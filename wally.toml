[package]
name = "endarke/sequence"
description = "A simple task sorting module, used to shorten the workload."
license = "MIT"
authors = ["EnDarke"]
version = "0.1.0"

registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/*", "wally.toml"]

[dependencies]
